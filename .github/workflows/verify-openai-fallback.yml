name: Verify OpenAI fallback

on:
  push:
    branches: [ feature/openai-provider-fallback ]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci || npm install
      - name: Syntax checks
        run: |
          node --check src/services/ai/providers/openai.js
          node --check src/services/ai/providerRouter.js
          node --check src/services/groq.js
      - name: Regression checks
        run: |
          node scripts/test-openai-fallback.js
          node scripts/test-completed-work-guard.js
